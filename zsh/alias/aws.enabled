alias aws-cli='docker-run --rm -t $(tty &>/dev/null && echo "-i") -e "AWS_REGION=${AWS_REGION}" -e "AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}" -e "AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}" mesosphere/aws-cli'
